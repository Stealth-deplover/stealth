# The installer creates this top-level file-provider reload sentinel. The
# route reconciler atomically replaces it after publishing generated files so
# Traefik notices changes under the recursively loaded generated directory.
