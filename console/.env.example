# Leave empty for the recommended same-origin deployment. The browser then
# calls /v1/* and the reverse proxy forwards that path to the Go API.
NEXT_PUBLIC_API_BASE_URL=

# Optional browser-visible label only; never put secrets in NEXT_PUBLIC_*.
NEXT_PUBLIC_APP_NAME=Stealth Console
