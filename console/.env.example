# Empty means same-origin requests to /v1/*.
NEXT_PUBLIC_API_BASE_URL=

# Only needed when the browser runs on a different origin during development.
# The Go API must include this origin in CONSOLE_CORS_ORIGINS.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

NEXT_PUBLIC_APP_NAME=Stealth Console
