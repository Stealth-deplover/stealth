"use client";

import Link from "next/link";
import { ArrowUpRight, Box, Database, FunctionSquare, Globe2, HardDrive, History, Users } from "lucide-react";
import { useProject, useProjectAudit, useProjectUsage } from "@/api/queries";
import { ErrorState } from "@/components/feedback/error-state";
import { LoadingState } from "@/components/feedback/loading-state";
import { PageHeader } from "@/components/page-header";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { CopyButton } from "@/components/copy-button";
import { formatBytes, formatCount, formatDate } from "@/lib/format";

function Metric({ label, value, icon: Icon, href }: { label: string; value: string | number; icon: typeof Box; href: string }) {
  return <Link href={href} className="group rounded-xl border border-stealth-border bg-stealth-panel p-4 transition hover:border-cyan-300/30 hover:bg-white/[0.03]"><div className="flex items-center justify-between"><p className="text-xs text-slate-500">{label}</p><Icon className="size-4 text-slate-600 transition group-hover:text-cyan-300" /></div><p className="mt-3 text-2xl font-semibold text-white">{value}</p><span className="mt-2 inline-flex items-center gap-1 text-[11px] text-slate-600 group-hover:text-cyan-300">Open resource <ArrowUpRight className="size-3" /></span></Link>;
}

export function ProjectOverviewView({ projectId, organizationId }: { projectId: string; organizationId: string }) {
  const project = useProject(projectId);
  const usage = useProjectUsage(projectId);
  const audit = useProjectAudit(projectId);
  if (project.error) return <ErrorState error={project.error} retry={() => project.refetch()} />;
  if (project.isPending) return <LoadingState rows={6} />;
  const current = project.data?.project;
  const usageData = usage.data?.usage;
  const base = `/organizations/${organizationId}/projects/${projectId}`;
  return <><PageHeader eyebrow="Project overview" title={current?.name ?? "Project"} description="A live read of the resources and activity returned by the Stealth API." actions={<Button asChild variant="outline"><Link href={`${base}/services`}><Box className="size-4" /> Open services</Link></Button>} /><div className="mb-7 flex flex-wrap items-center gap-3"><Badge variant="success"><span className="size-1.5 rounded-full bg-current" /> API connected</Badge><span className="text-xs text-slate-600">Project data is sourced from live endpoints.</span>{usageData?.captured_at ? <span className="text-xs text-slate-700">Snapshot {formatDate(usageData.captured_at)}</span> : null}<span className="inline-flex items-center gap-1.5 text-xs text-slate-600"><span className="font-mono text-[11px] text-slate-500">{projectId.slice(0, 8)}…</span><CopyButton value={projectId} label="Copy project ID" className="size-6 text-slate-600 hover:text-slate-200" /></span></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Functions" value={formatCount(usageData?.function_count)} icon={FunctionSquare} href={`${base}/functions`} /><Metric label="Sites" value={formatCount(usageData?.site_count)} icon={Globe2} href={`${base}/sites`} /><Metric label="Databases" value={formatCount(usageData?.database_count)} icon={Database} href={`${base}/databases`} /><Metric label="Storage" value={formatBytes(usageData?.storage_bytes)} icon={HardDrive} href={`${base}/storage`} /></div><div className="mt-6 grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><Card><CardHeader className="flex-row items-center justify-between"><div><CardTitle>Resource posture</CardTitle><p className="mt-1 text-xs text-slate-500">Actual counts and usage from the current snapshot.</p></div><History className="size-4 text-slate-600" /></CardHeader><CardContent><div className="grid gap-3 sm:grid-cols-2">{[["Function artifact", formatBytes(usageData?.function_artifact_bytes), formatBytes(usageData?.function_quota_bytes)], ["Site artifact", formatBytes(usageData?.site_artifact_bytes), formatBytes(usageData?.site_quota_bytes)], ["Database rows", formatCount(usageData?.database_row_count), "rows"], ["Application users", formatCount(usageData?.application_users), "users"], ["API requests (30d)", formatCount(usageData?.api_request_count_30d), "requests"], ["Function failures (30d)", formatCount(usageData?.function_failure_count_30d), "failures"]].map(([label, value, note]) => <div key={label} className="rounded-lg border border-stealth-border bg-black/10 p-3.5"><p className="text-xs text-slate-500">{label}</p><div className="mt-2 flex items-end justify-between gap-2"><p className="text-lg font-semibold text-white">{value}</p><span className="text-[10px] text-slate-600">{note}</span></div></div>)}</div></CardContent></Card><Card><CardHeader><CardTitle>Recent activity</CardTitle><p className="mt-1 text-xs text-slate-500">Project audit events, newest first.</p></CardHeader><CardContent className="space-y-4">{audit.isPending ? <LoadingState rows={4} /> : audit.data?.events?.length ? audit.data.events.slice(0, 6).map((event) => <div key={event.id} className="flex gap-3"><span className="mt-1.5 size-1.5 shrink-0 rounded-full bg-cyan-300" /><div className="min-w-0"><p className="truncate text-sm text-slate-200">{event.action}</p><p className="mt-0.5 text-xs text-slate-600">{event.target_type} · {formatDate(event.created_at)}</p></div></div>) : <p className="text-sm text-slate-500">No audit events returned yet.</p>}</CardContent></Card></div><p className="mt-6 flex items-center gap-2 rounded-lg border border-stealth-border bg-white/[0.02] px-3 py-2.5 text-xs leading-5 text-slate-600"><Users className="size-3.5 shrink-0" /> Backend does not expose a unified project health or deployment feed; usage, activity, and health are intentionally limited to measured backend data.</p></>;
}
